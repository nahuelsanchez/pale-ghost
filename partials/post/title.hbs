<div class="post-title">
    {{#is "post, page"}}
        <h1 class="post-title">{{title}}</h1>
    {{else}}
        <h2 class="post-title">
            <a href="{{url}}" title="{{title}}">{{title}}</a>
        </h2>
    {{/is}}
</div>